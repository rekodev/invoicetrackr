{
  "name": "invoice-app-server",
  "version": "1.0.0",
  "description": "The backend for rekodev's invoice app",
  "main": "dist/app.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "tsc && node dist/app.js",
    "dev": "tsc && nodemon dist/app.js"
  },
  "author": "rekodev",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.11.30",
    "dotenv": "^16.4.5",
    "nodemon": "^3.1.0",
    "typescript": "^5.4.3"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "fastify": "^4.26.2"
  }
}
